<div class="categories">
    {% for category in categories  %}
 <div class="dropdown-item"><a href="{{ path('category_show', {'id': category.id}) }}">{{ category.name }}</a></div>
    {% endfor %}
</div>